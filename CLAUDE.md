# Italy Tours Platform

## MANDATORY: Use CRG Tools First

**CRITICAL: You MUST use code-review-graph (CRG) tools for ALL code exploration. Never use Read/Glob/Grep directly.**

### Required Workflow

1. **Every file read** → Use `get_review_context` or `query_graph` first
2. **Code review** → Use `detect_changes` 
3. **Finding tests** → Use `query_graph pattern="tests_for"`
4. **Understanding code** → Use `query_graph pattern="callees_of"` or `callers_of"`
5. **Impact analysis** → Use `get_impact_radius`

### Why This Matters

- **CRG**: 6.8x fewer tokens on file reads
- **token-optimizer**: 95% compression on tool outputs
- **mem0**: Persistent memory across sessions
- **Combined**: Up to 49x token reduction + memory that persists

### When You MUST Use CRG

| Task | CRG Tool |
|------|----------|
| Read any file | `get_review_context` |
| Find function usages | `query_graph` with callers_of |
| Find tests for code | `query_graph pattern="tests_for"` |
| Understand imports | `query_graph pattern="imports_of"` |
| Review changes | `detect_changes` |
| See impact radius | `get_impact_radius` |
| Search code | `semantic_search_nodes` |

### Memory: Log Your Decisions

After completing a significant task, log the decision:
```
mem0ai.add("Added auth middleware. Why: Need rate limiting. Files: auth.js, rate.js. Context: 3 endpoints.")
```

This builds persistent memory for future sessions.

### Fallback Only If

- CRG returns insufficient info
- File not in graph yet
- Need very recent changes not yet in graph

### Available Tools

```
CRG (code-review-graph):
  detect_changes        - Risk-scored change review
  get_review_context   - Token-efficient snippets
  get_impact_radius     - Blast radius analysis  
  query_graph           - Callers, callees, tests, imports
  semantic_search      - Find by meaning
  build_or_update_graph - Refresh graph
  list_graph_stats      - Check graph health

Memory (mem0):
  mem0.add              - Store decision/context
  mem0.search           - Retrieve past context
```

### Quick Commands

```bash
# Build graph when new code is added
code-review-graph build

# Check graph stats
code-review-graph list_graph_stats
```

---

*This file auto-generated by crg-dev-kit.*

---

## AI Tooling Guidelines (added 2026-09-14)

> **Note on the section above:** it was auto-generated by a third-party tool
> (`crg-dev-kit`) and makes specific, unverified efficiency claims ("6.8x", "95%",
> "49x"). It's left in place rather than removed, but treat it as one option among
> several below, not as a mandatory override of normal file tools — an AI assistant
> should still use whatever tool actually gets the job done, including plain file
> reads when a graph/index isn't available or is stale.

The four tools below were added deliberately, one at a time, each with its own
config and doc. See each linked doc for setup steps and the reasoning specific to
this repo (a pnpm/Turborepo monorepo with a Next.js app in `apps/web`, several
large (30–48 KB) per-tour content files under `apps/web/src/lib/`, and no current
use of the Anthropic API in production code).

1. **Repo mapping & semantic code search** — Serena MCP server (registered in
   `.mcp.json`) plus Aider-style repo-map thinking for symbol-level navigation.
   See [`docs/ai-tooling/repo-navigation.md`](docs/ai-tooling/repo-navigation.md).
2. **Repomix** — packs the repo into a condensed digest for one-shot context.
   See [`docs/ai-tooling/repomix.md`](docs/ai-tooling/repomix.md).
3. **Prompt caching** — guidance for structuring AI-assisted sessions in this repo,
   and a ready pattern for if/when an Anthropic API call is added to the product.
   See [`docs/ai-tooling/prompt-caching.md`](docs/ai-tooling/prompt-caching.md).
4. **Persistent memory (mem0 / Zep)** — cross-session context for AI assistants
   working in this repo. See [`docs/ai-tooling/persistent-memory.md`](docs/ai-tooling/persistent-memory.md)
   (status: config drafted, activation pending API keys).
